# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Terraform
.terraform/ # Ignore the working directory for providers and modules
*.tfstate
*.tfstate.*.backup
.terraform.lock.hcl # For Terraform 0.14+ CLI versions
crash.log
crash.*.log
*.tfvars # Often contain secrets, use .tfvars.example for templates
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
*.tfplan # Local plan files can be large and are environment-specific

# Terraform generated files for lab sessions (your custom rule)
terraform/generated/

# Ignore downloaded provider plugins (even if not in .terraform directory by mistake)
terraform-provider-*

# If you have a .tfenv file for managing terraform versions
.terraform-version
.tfenvrc
